import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.depa.app',
  appName: 'Depa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
